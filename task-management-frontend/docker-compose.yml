version: '3.8'
services:
  frontend:
    build: .
    command: npm run dev
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    environment:
      VITE_API_URL: http://backend:3000/graphql
volumes:
  node_modules:
