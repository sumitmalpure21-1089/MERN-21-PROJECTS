# Task Management Backend (Rails + GraphQL)

A Ruby on Rails API backend for a personal task management application, using GraphQL.

### Prerequisites

- **Docker Desktop**. (only requirment)

## Quick Start

docker compose ps
docker compose logs backend --tail 30
docker compose up --build
docker compose down -v
